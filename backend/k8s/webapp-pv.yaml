apiVersion: v1
kind: PersistentVolume
metadata:
  name: webapp-pv
  labels:
    name: database-volume
spec:
  capacity:
    storage: 100Mi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Recycle
